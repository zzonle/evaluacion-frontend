tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#1f2937', // Gris oscuro (formal)
            accent: '#3b82f6',  // Azul serio
            soft: '#f3f4f6'     // Fondo suave
          }
        }
      }
    }
